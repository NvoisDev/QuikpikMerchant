🧾 Prompt: Stripe Mode Separation Audit (Test vs Live)

🎯 Objective

Confirm that test accounts and live accounts are always handled correctly and safely across ALL Stripe interactions, with no leakage between environments.

🚨 NON-NEGOTIABLE RULES
Do NOT modify business logic
Do NOT change Stripe keys or environment variables
Do NOT break any payment, subscription, or refund flows
This is a read + verify + report task only
🧱 PART 1 — Stripe Client Usage
Search the entire codebase for:
new Stripe(
stripe.
Confirm:
✅ Stripe is only initialised in one place (stripeConfig.ts)

✅ ALL Stripe calls use:

const stripe = getStripeClient(...)
❌ No route imports or uses:
global stripe
DO_NOT_USE_stripe
any static/shared Stripe instance

👉 Output:

Total Stripe calls
Any violations (file + line)
🧱 PART 2 — Account-Aware Context

For EVERY Stripe call:

Confirm it derives mode using:

getStripeClient(Boolean(user.isTestAccount))

or equivalent wholesaler lookup

Validate:
req.user.isTestAccount is available OR
wholesaler is fetched BEFORE Stripe is called

Check for risky patterns:

Boolean(wholesaler?.isTestAccount)

👉 If wholesaler is null → defaults to LIVE

👉 Output:

List of all safe calls
List of any “fail-to-live” risks
🧱 PART 3 — Subscription Service Check (CRITICAL)

Inspect:

server/subscription-service.ts

Confirm:

❌ No module-level Stripe client like:

const stripe = getStripeClient()

✅ Every method accepts or derives:

isTestAccount
✅ Stripe client is created INSIDE each method

👉 Output:

Whether subscription flows are account-aware
Any risk of test accounts hitting LIVE Stripe
🧱 PART 4 — Full Flow Coverage

Verify Stripe mode handling across:

Flow	Expected
Customer checkout	Account-aware
Quick quotes	Account-aware
Payment reminders	Account-aware
Refunds	Account-aware
Subscriptions	Account-aware
Admin actions	Uses correct wholesaler context

👉 Output:

✅/❌ per flow
Any gaps
🧱 PART 5 — Behaviour Validation

Confirm logic:

const useLive = isLiveMode() && !forceTest;

Test scenarios:

Scenario	Expected
Test account + LIVE platform	TEST Stripe
Live account + LIVE platform	LIVE Stripe
Test account + TEST platform	TEST Stripe

👉 Output:

Confirm all scenarios behave correctly
🧱 PART 6 — Logging (Optional but Recommended)

Check if logs show:

Stripe client: 🔵 LIVE
Stripe client: 🟢 TEST

👉 Recommend:

Add wholesalerId to logs (if missing)
🧪 FINAL OUTPUT FORMAT

Agent should return:

✅ Total Stripe calls: X
✅ Account-aware: X/X
⚠️ Risks found: X

Breakdown:
- Safe areas:
- Risk areas:
- Subscription service status:
- Any fail-to-live issues:

Final verdict:
🟢 SAFE / 🟡 CONDITIONAL / 🔴 NOT SAFE
💡 Why this matters (quick real talk)

You’ve already seen the exact failure mode:

Test account → accidentally hits LIVE Stripe → boom 💥

This prompt makes sure:

Every single Stripe call respects account context
No silent fallback to LIVE
No hidden “one file forgot” situation